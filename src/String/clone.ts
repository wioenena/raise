'use strict';

/**
 * Copies the string.
 * @param content - The string to be copied.
 * @returns {string}
 * @example
 * clone("Hello"); // Hello
 */
export default function clone(content: string): string {
    return `${content}`;
}
