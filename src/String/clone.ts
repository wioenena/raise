'use strict';

/**
 * Copies the string.
 * @param content - Main string.
 * @returns {string}
 * @example
 * clone("Hello"); // Hello
 */
export function clone(content: string) {
    return `${content}`;
}
