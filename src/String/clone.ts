'use strict';

/**
 * Copies the string.
 * @param content - Main string.
 * @returns {string}
 * @example
 * clone("Hello"); // Hello
 */
export default function clone(content: string) {
    return `${content}`;
}
